package com.cra.princess.pathplanner.component

import com.cra.princess.core.PrincessFeatureCollection

class PathPlannerEnvironment extends PrincessFeatureCollection(List.empty) {
    // TODO if this stays empty, then we should have an EmptyEnvironment object instead
}
