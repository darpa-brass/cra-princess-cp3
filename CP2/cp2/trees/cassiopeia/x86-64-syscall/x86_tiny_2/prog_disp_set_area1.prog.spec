PRE:
	RDI points to the disp struct
	OFFSETOF_DISP_ENABLED_AREA is correct
	after jump to save_enabled


POST:
(VERSION 1)
	RDI points to the disp struct enabled area

(VERSION 2)
	RDI points to the disp struct enabled area