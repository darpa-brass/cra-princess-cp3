PRE:
	R14 points to dispatcher saved area (or it could represent stack pointer)

POST:
(VERSION 1)
	saved_area.R0 = R0
	(or R0 stored in stack)

(VERSION 2)
	saved_area.R0 = R0
	(or R0 stored in stack)
